/* GAMEOVER SCENE CLASS */
export class GameoverScene extends Phaser.Scene
{
    constructor() {
        super({ key: 'GameoverScene', active: false });
    }

    preload() {
        /* ASSETS GAMEOVER SCENE */
        console.log("Loading GameoverScene...");
    }

    create() {
        
    }
}